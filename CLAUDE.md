For Claude Code

Read docs/CLAUDE-GUIDE.md. Before release, use `deno doc` to generate API documentation into
`docs/api` directory.

eSolia logos are in `/assets`. Use tools like `sharp` or `svgo` + `sharp-cli` for conversion of the
"symbol" flavor to needed favicons.
